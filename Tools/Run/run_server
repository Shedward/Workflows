#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
ROOT_DIR="$SCRIPT_DIR/../.."

"$SCRIPT_DIR/build"

EXECUTABLE="$ROOT_DIR/.build/DerivedData/Build/Products/Debug/workflow-server-testing"

echo "Starting server..."
exec "$EXECUTABLE"
